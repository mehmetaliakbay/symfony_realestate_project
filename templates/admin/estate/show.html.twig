{% extends 'base.html.twig' %}

{% block title %}Estate{% endblock %}

{% block body %}
    <h1>Estate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estate.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ estate.title }}</td>
            </tr>
            <tr>
                <th>Keywords</th>
                <td>{{ estate.keywords }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ estate.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ estate.image }}</td>
            </tr>
            <tr>
                <th>Star</th>
                <td>{{ estate.star }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ estate.adress }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ estate.phone }}</td>
            </tr>
            <tr>
                <th>Fax</th>
                <td>{{ estate.fax }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ estate.email }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ estate.city }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ estate.location }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ estate.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ estate.createdAt ? estate.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ estate.updatedAt ? estate.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estate_index') }}">back to list</a>

    <a href="{{ path('estate_edit', {'id': estate.id}) }}">edit</a>

    {{ include('estate/_delete_form.html.twig') }}
{% endblock %}
